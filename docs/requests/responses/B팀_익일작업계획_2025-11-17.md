# B팀 익일 작업 계획 (2025-11-17)

**작성일**: 2025-11-16
**작성자**: B팀 (Backend)
**전일 작업**: Phase 1-3 완료 (ee19f82)

---

## 📋 작업 전 체크리스트

### 🔧 환경 설정 확인 (최우선)

**Windows 환경변수 정리 필수**:

```powershell
# PowerShell 관리자 권한으로 실행

# 1. 현재 설정 확인
[Environment]::GetEnvironmentVariable("OLLAMA_BASE_URL", "User")
[Environment]::GetEnvironmentVariable("OLLAMA_BASE_URL", "Machine")
[Environment]::GetEnvironmentVariable("COMFYUI_BASE_URL", "User")
[Environment]::GetEnvironmentVariable("COMFYUI_BASE_URL", "Machine")

# 2. 삭제 (구 IP가 설정되어 있는 경우)
[Environment]::SetEnvironmentVariable("OLLAMA_BASE_URL", $null, "User")
[Environment]::SetEnvironmentVariable("OLLAMA_BASE_URL", $null, "Machine")
[Environment]::SetEnvironmentVariable("COMFYUI_BASE_URL", $null, "User")
[Environment]::SetEnvironmentVariable("COMFYUI_BASE_URL", $null, "Machine")

# 3. 확인 (null 또는 빈 값이어야 함)
[Environment]::GetEnvironmentVariable("OLLAMA_BASE_URL", "User")
[Environment]::GetEnvironmentVariable("OLLAMA_BASE_URL", "Machine")
```

**재시작**:
- ✅ 모든 터미널 종료
- ✅ VSCode 완전 종료
- ✅ (권장) PC 재부팅 또는 로그아웃/로그인

### 📚 필수 읽기 문서

작업 시작 전 반드시 읽어야 할 문서들:

1. **[Phase 1-3 완료보고](./B팀_Phase1-3_완료보고_2025-11-16.md)** ⭐
   - 어제 작업 내용 전체 요약
   - 발견된 환경변수 이슈 상세 설명
   - Live 모드 검증 체크리스트

2. **[ARCH-002: LLM Gateway Architecture](../architecture/ARCH-002-LLM_Gateway_Architecture.md)**
   - Gateway 전체 구조
   - Provider 패턴
   - Router 역할

3. **[SPEC-001: API Specification](../architecture/SPEC-001-LLM_Gateway_API_Specification.md)**
   - API 엔드포인트 명세
   - 요청/응답 포맷
   - 에러 처리 규격

---

## 🎯 Phase 1-4: Live 모드 최종 검증 및 마무리

### Task 1: 환경 검증 (30분)

**목표**: 환경변수 정리 후 올바른 IP로 로드되는지 확인

```bash
# 서버 시작
cd /k/sparklio_ai_marketing_studio/backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

**체크포인트**:

```bash
# 1. 설정 확인
curl http://localhost:8000/api/v1/debug/settings | python -m json.tool

# ✅ 예상 결과:
{
  "generator_mode": "mock",
  "ollama_base_url": "http://100.120.180.42:11434",  # ← 올바른 IP
  "ollama_timeout": 120,
  "ollama_default_model": "qwen2.5:7b",
  "comfyui_base_url": "http://100.120.180.42:8188"
}
```

**⚠️ 만약 여전히 `192.168.0.100`이 나온다면**:
- 터미널을 완전히 재시작했는지 확인
- PC 재부팅 후 재시도
- 또는 임시 해결책: `config.py`에서 `.env` 파일 우선 로드하도록 수정

### Task 2: Ollama 연결 테스트 (30분)

**목표**: FastAPI 컨텍스트에서 Ollama 서버 정상 연결 확인

```bash
# 연결 테스트
curl http://localhost:8000/api/v1/debug/ollama | python -m json.tool

# ✅ 예상 결과:
{
  "target_url": "http://100.120.180.42:11434/api/tags",
  "success": true,
  "models": ["qwen2.5:7b", "qwen2.5:14b", "llama2:7b", ...],
  "total_models": 5,
  "elapsed_ms": 245.67
}
```

**❌ 실패 시 디버깅**:
1. Desktop GPU 서버(100.120.180.42) 상태 확인
2. Tailscale VPN 연결 확인
3. Ollama 서비스 실행 여부 확인:
   ```bash
   # Desktop에서
   curl http://localhost:11434/api/tags
   ```

### Task 3: Ollama 생성 테스트 (30분)

**목표**: 실제 텍스트 생성 파이프라인 전체 검증

```bash
# 생성 테스트
curl http://localhost:8000/api/v1/debug/ollama/generate | python -m json.tool

# ✅ 예상 결과:
{
  "success": true,
  "response_text": "Hello! How can I help you today?",
  "full_length": 156,
  "elapsed_ms": 1234.56
}
```

**성능 확인**:
- 응답 시간: 1~3초 내외 (qwen2.5:7b 기준)
- 더 오래 걸리면 Desktop GPU 부하 확인

### Task 4: Live 모드 E2E 테스트 (1시간)

**목표**: LLM Gateway를 통한 실제 Ollama 호출 검증

#### 4-1. Live 모드 전환

`.env` 파일 수정:
```bash
# backend/.env
GENERATOR_MODE=live  # mock → live로 변경
```

서버 자동 재로드 확인 (uvicorn --reload)

#### 4-2. LLM Gateway 호출

```bash
# SNS 콘텐츠 생성 테스트
curl -X POST http://localhost:8000/api/v1/llm/generate \
  -H "Content-Type: application/json" \
  -d '{
    "role": "copywriter",
    "task": "sns",
    "payload": {
      "product": "무선 이어폰",
      "target": "2030 여성",
      "platform": "instagram"
    },
    "mode": "json"
  }' | python -m json.tool
```

**✅ 성공 기준**:
```json
{
  "provider": "ollama",
  "model": "qwen2.5:7b",
  "usage": {
    "prompt_tokens": 50,
    "completion_tokens": 120,
    "total_tokens": 170
  },
  "output": {
    "platform": "instagram",
    "caption": "🎧 새로운 음악 경험...",
    "hashtags": ["무선이어폰", "프리미엄사운드"],
    "call_to_action": "지금 바로 확인하기 👉"
  },
  "meta": {
    "latency_ms": 1234.56,
    "mode": "json",
    "role": "copywriter",
    "task": "sns"
  }
}
```

**검증 포인트**:
- ✅ `provider: "ollama"` (Mock이 아님)
- ✅ `model: "qwen2.5:7b"` (실제 모델)
- ✅ `output`이 실제 LLM이 생성한 내용 (Mock 템플릿 아님)
- ✅ `latency_ms`가 1~3초 범위
- ✅ JSON 구조가 올바름

#### 4-3. 다양한 시나리오 테스트

```bash
# 1. 상품 상세 설명 생성
curl -X POST http://localhost:8000/api/v1/llm/generate \
  -H "Content-Type: application/json" \
  -d '{
    "role": "copywriter",
    "task": "product_detail",
    "payload": {"product": "스마트워치"},
    "mode": "json"
  }' | python -m json.tool

# 2. 브랜드킷 생성
curl -X POST http://localhost:8000/api/v1/llm/generate \
  -H "Content-Type: application/json" \
  -d '{
    "role": "strategist",
    "task": "brand_kit",
    "payload": {"brand": "에코테크"},
    "mode": "json"
  }' | python -m json.tool

# 3. Text 모드 테스트
curl -X POST http://localhost:8000/api/v1/llm/generate \
  -H "Content-Type: application/json" \
  -d '{
    "role": "copywriter",
    "task": "sns",
    "payload": {"product": "친환경 텀블러"},
    "mode": "text"
  }' | python -m json.tool
```

#### 4-4. 에러 케이스 테스트

```bash
# 존재하지 않는 모델 요청
curl -X POST http://localhost:8000/api/v1/llm/generate \
  -H "Content-Type: application/json" \
  -d '{
    "role": "copywriter",
    "task": "sns",
    "payload": {"product": "테스트"},
    "mode": "json",
    "options": {"model": "nonexistent-model"}
  }' | python -m json.tool

# ✅ 예상: 적절한 에러 응답 (ProviderError)
```

### Task 5: 성능 및 안정성 검증 (30분)

#### 5-1. 부하 테스트

```bash
# 연속 5회 요청
for i in {1..5}; do
  echo "=== Request $i ==="
  curl -X POST http://localhost:8000/api/v1/llm/generate \
    -H "Content-Type: application/json" \
    -d '{
      "role": "copywriter",
      "task": "sns",
      "payload": {"product": "테스트 상품 '$i'"},
      "mode": "json"
    }' -w "\nTime: %{time_total}s\n"
  sleep 1
done
```

**확인사항**:
- 모든 요청 성공
- 응답 시간 일관성
- 메모리 누수 없음

#### 5-2. 로그 분석

서버 로그에서 확인:
```
INFO: Ollama API call: url=http://100.120.180.42:11434/api/generate, model=qwen2.5:7b
INFO: Ollama success: qwen2.5:7b - elapsed=1.23s
INFO: LLM Success: ollama/qwen2.5:7b - elapsed=1.25s, tokens=170
```

---

## 📝 Task 6: 문서화 및 커밋 (1시간)

### 6-1. Phase 1-4 완료 보고서 작성

**파일**: `docs/requests/responses/B팀_Phase1-4_완료보고_2025-11-17.md`

**포함 내용**:
- 환경변수 이슈 해결 과정
- Live 모드 검증 결과
- 성능 테스트 결과
- Mock vs Live 비교 분석
- Phase 1 전체 회고

### 6-2. Git 커밋

```bash
git add docs/requests/responses/B팀_Phase1-4_완료보고_2025-11-17.md
git commit -m "docs(gateway): Phase 1-4 완료 보고 - Live 모드 검증 완료

- Windows 환경변수 이슈 해결
- Live 모드 E2E 테스트 성공
- 성능 및 안정성 검증 완료
- Phase 1 전체 완료"
```

---

## 🚀 Phase 2 준비 (시간 여유 시)

Phase 1이 성공적으로 완료되면 Phase 2 준비 작업:

### Router 고도화 (Phase 2-1 예상)

**목표**: 작업별 최적 모델 자동 선택

```python
# router.py 확장 아이디어
TASK_MODEL_MAP = {
    "sns": "qwen2.5:7b",        # 빠른 응답 필요
    "product_detail": "qwen2.5:14b",  # 품질 우선
    "brand_kit": "qwen2.5:14b",
    "review": "qwen2.5:7b"
}
```

### 에러 핸들링 강화

- Retry 로직 추가 (지수 백오프)
- Circuit Breaker 패턴
- Fallback Provider

### 모니터링 추가

- Prometheus metrics
- 응답 시간 히스토그램
- 에러율 추적

---

## ⚠️ 예상 이슈 및 해결 방안

### Issue 1: 환경변수 여전히 로드 안 됨

**증상**: 재부팅 후에도 여전히 `192.168.0.100` 로드

**해결**:
```python
# config.py 임시 수정 (긴급)
class Settings(BaseSettings):
    OLLAMA_BASE_URL: str = "http://100.120.180.42:11434"

    class Config:
        env_file = ".env"
        env_file_encoding = 'utf-8'
        case_sensitive = True
        # 환경변수보다 .env 우선하도록 강제
        @classmethod
        def customise_sources(cls, init_settings, env_settings, file_secret_settings):
            return (
                init_settings,
                file_secret_settings,  # .env 우선
                env_settings,
            )
```

### Issue 2: Ollama 서버 연결 불안정

**증상**: 간헐적 ConnectError

**확인**:
1. Tailscale 상태: `tailscale status`
2. Desktop GPU 서버 네트워크
3. Ollama 서비스 재시작

**임시 해결**: Retry 로직 추가

### Issue 3: JSON 파싱 실패

**증상**: Ollama 응답이 JSON이 아님

**원인**: 모델이 JSON 형식을 지키지 않음

**해결**:
- 프롬프트에 JSON 구조 명시 강화
- 파싱 실패 시 텍스트 모드로 폴백

---

## 📊 성공 기준

### Phase 1 전체 완료 조건

- ✅ Mock 모드 정상 동작
- ✅ Live 모드 정상 동작 (Ollama)
- ✅ Debug 엔드포인트 3종 작동
- ✅ E2E 테스트 모든 시나리오 통과
- ✅ 성능 기준 충족 (응답 시간 < 3초)
- ✅ 에러 핸들링 적절
- ✅ 문서화 완료

### 품질 기준

- 코드 리뷰 가능 상태
- 로깅 충분히 상세
- 에러 메시지 명확
- API 명세 준수

---

## 📅 타임라인

| 시간 | 작업 | 비고 |
|------|------|------|
| 09:00-09:30 | 환경 검증 | Task 1 |
| 09:30-10:00 | Ollama 연결 테스트 | Task 2 |
| 10:00-10:30 | Ollama 생성 테스트 | Task 3 |
| 10:30-11:30 | Live 모드 E2E | Task 4 |
| 11:30-12:00 | 성능 검증 | Task 5 |
| 13:00-14:00 | 문서화 및 커밋 | Task 6 |
| 14:00-17:00 | Phase 2 준비 (선택) | 여유 시 |

---

## 🔗 빠른 참조

### 주요 명령어

```bash
# 서버 시작
cd /k/sparklio_ai_marketing_studio/backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 설정 확인
curl http://localhost:8000/api/v1/debug/settings | python -m json.tool

# Ollama 연결
curl http://localhost:8000/api/v1/debug/ollama | python -m json.tool

# 생성 테스트
curl http://localhost:8000/api/v1/debug/ollama/generate | python -m json.tool

# LLM Gateway (Live)
curl -X POST http://localhost:8000/api/v1/llm/generate \
  -H "Content-Type: application/json" \
  -d '{"role": "copywriter", "task": "sns", "payload": {"product": "테스트"}, "mode": "json"}' \
  | python -m json.tool
```

### 주요 파일

- `backend/.env` - 환경 설정
- `backend/app/core/config.py` - 설정 클래스
- `backend/app/services/llm/providers/ollama.py` - Ollama Provider
- `backend/app/services/llm/gateway.py` - LLM Gateway
- `backend/app/api/v1/endpoints/debug.py` - Debug API

### 서버 정보

- **Mac mini (API 서버)**: 100.123.51.5
- **Desktop GPU (Ollama)**: 100.120.180.42:11434
- **Desktop GPU (ComfyUI)**: 100.120.180.42:8188

---

## ✅ 체크리스트

작업 시작 전:
- [ ] Windows 환경변수 정리 완료
- [ ] PC 재부팅 또는 로그아웃/로그인
- [ ] Phase 1-3 완료보고 읽기
- [ ] ARCH-002, SPEC-001 재확인

작업 중:
- [ ] Task 1: 환경 검증 ✅
- [ ] Task 2: Ollama 연결 테스트 ✅
- [ ] Task 3: Ollama 생성 테스트 ✅
- [ ] Task 4: Live 모드 E2E ✅
- [ ] Task 5: 성능 검증 ✅
- [ ] Task 6: 문서화 및 커밋 ✅

작업 완료 후:
- [ ] Phase 1-4 완료보고 작성
- [ ] Git 커밋 및 푸시
- [ ] 다음 작업 계획 작성
- [ ] 팀 공유

---

**작성자**: B팀 (Backend)
**작성일**: 2025-11-16
**다음 검토일**: 2025-11-17 오후
