# 🚀 Sparklio AI 마케팅 스튜디오 - 개발 가이드

## 목차

1. [시작하기](#시작하기)
2. [개발 환경 설정](#개발-환경-설정)
3. [프로젝트 구조 이해](#프로젝트-구조-이해)
4. [코딩 컨벤션](#코딩-컨벤션)
5. [개발 워크플로우](#개발-워크플로우)
6. [테스트 작성](#테스트-작성)
7. [배포 프로세스](#배포-프로세스)
8. [트러블슈팅](#트러블슈팅)

---

## 시작하기

### 필수 요구사항

#### 소프트웨어
- **Python 3.11+**: 백엔드 개발
- **Node.js 18+**: 프론트엔드 개발
- **Docker & Docker Compose**: 컨테이너 환경
- **Git**: 버전 관리
- **PostgreSQL 15+**: 데이터베이스
- **Redis 7+**: 캐시 및 큐

#### 하드웨어 (권장)
- **CPU**: 8코어 이상
- **RAM**: 16GB 이상
- **GPU**: NVIDIA RTX 4060 이상 (이미지/영상 생성용)
- **Storage**: 100GB 이상 여유 공간

### 저장소 클론

```bash
# GitHub에서 클론
git clone https://github.com/ssdavo34/Sparklio_ai_marketing_studio.git
cd Sparklio_ai_marketing_studio

# 개발 브랜치 확인
git checkout -b feature/your-feature-name
```

---

## 개발 환경 설정

### 1. 백엔드 설정 (FastAPI)

#### 가상 환경 생성 및 활성화

```bash
# 백엔드 디렉토리로 이동
cd backend

# Python 가상 환경 생성
python -m venv venv

# 가상 환경 활성화 (Linux/Mac)
source venv/bin/activate

# 가상 환경 활성화 (Windows)
# venv\Scripts\activate

# 의존성 설치
pip install -r requirements.txt
```

#### 환경 변수 설정

`.env` 파일을 생성하고 다음 내용을 추가합니다:

```bash
# 데이터베이스 설정
DATABASE_URL=postgresql://user:password@localhost:5432/sparklio_db

# Redis 설정
REDIS_URL=redis://localhost:6379/0

# LLM API 키
OPENAI_API_KEY=your_openai_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key
GOOGLE_API_KEY=your_google_api_key

# S3/MinIO 설정
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=sparklio-assets

# JWT 설정
SECRET_KEY=your_secret_key_here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 환경 설정
ENVIRONMENT=development
DEBUG=True
```

#### 데이터베이스 초기화

```bash
# Alembic 마이그레이션 실행
alembic upgrade head

# 초기 데이터 시딩 (선택사항)
python scripts/seed_database.py
```

#### 개발 서버 실행

```bash
# FastAPI 개발 서버 시작 (핫 리로드 활성화)
uvicorn backend.api.main:app --reload --host 0.0.0.0 --port 8000
```

서버가 시작되면 다음 주소에서 접근 가능합니다:
- **API**: http://localhost:8000
- **API 문서 (Swagger)**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### 2. 프론트엔드 설정 (Next.js)

```bash
# 프론트엔드 디렉토리로 이동
cd frontend

# 의존성 설치
npm install
# 또는
yarn install

# 환경 변수 설정
cp .env.example .env.local
```

`.env.local` 파일 내용:

```bash
# API 엔드포인트
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws

# 환경
NODE_ENV=development
```

#### 개발 서버 실행

```bash
# Next.js 개발 서버 시작
npm run dev
# 또는
yarn dev
```

프론트엔드는 http://localhost:3000에서 실행됩니다.

### 3. Docker Compose로 전체 환경 실행

전체 스택을 한 번에 실행하려면:

```bash
# 프로젝트 루트에서 실행
docker-compose up -d

# 로그 확인
docker-compose logs -f

# 특정 서비스만 재시작
docker-compose restart backend

# 전체 종료
docker-compose down
```

---

## 프로젝트 구조 이해

상세한 프로젝트 구조는 [`docs/프로젝트_구조.md`](../프로젝트_구조.md)를 참고하세요.

### 주요 디렉토리

```
├── backend/        # FastAPI 백엔드
├── frontend/       # Next.js 프론트엔드
├── agents/         # 멀티 에이전트 시스템
├── docs/           # 문서
├── config/         # 환경별 설정
├── scripts/        # 자동화 스크립트
└── tests/          # 테스트 코드
```

---

## 코딩 컨벤션

### Python (백엔드)

#### 1. 스타일 가이드

**PEP 8**을 기본으로 따르며, 다음 도구를 사용합니다:

```bash
# Black: 자동 포맷팅
black backend/

# isort: import 정렬
isort backend/

# flake8: 린팅
flake8 backend/

# mypy: 타입 체킹
mypy backend/
```

#### 2. 네이밍 컨벤션

```python
# 클래스: PascalCase
class BrandAnalyzer:
    pass

# 함수/변수: snake_case
def generate_marketing_brief():
    user_input = "example"

# 상수: UPPER_SNAKE_CASE
MAX_RETRIES = 3
API_TIMEOUT = 30

# Private 메서드/변수: 앞에 언더스코어
def _internal_helper():
    pass
```

#### 3. 주석 및 Docstring

**모든 함수와 클래스에는 한글 주석을 필수로 작성합니다.**

```python
def create_brand_kit(logo_url: str, website_url: str) -> dict:
    """
    브랜드 키트를 생성합니다.

    로고와 웹사이트 URL을 분석하여 브랜드의 컬러, 폰트, 톤앤매너를
    자동으로 추출합니다.

    Args:
        logo_url (str): 브랜드 로고 이미지 URL
        website_url (str): 브랜드 웹사이트 주소

    Returns:
        dict: 브랜드 키트 정보
            - colors: 브랜드 컬러 팔레트
            - fonts: 추천 폰트 목록
            - tone: 톤앤매너 설명
            - keywords: 핵심 키워드

    Raises:
        ValueError: URL이 유효하지 않은 경우
        APIError: 외부 API 호출 실패 시

    Example:
        >>> kit = create_brand_kit(
        ...     logo_url="https://example.com/logo.png",
        ...     website_url="https://example.com"
        ... )
        >>> print(kit['colors'])
        ['#FF5733', '#C70039', '#900C3F']
    """
    # 로고 이미지 다운로드 및 분석
    logo_data = download_image(logo_url)

    # 컬러 추출
    colors = extract_colors_from_image(logo_data)

    # 웹사이트 크롤링 및 분석
    website_content = scrape_website(website_url)
    tone = analyze_brand_tone(website_content)

    return {
        "colors": colors,
        "fonts": ["Pretendard", "Noto Sans KR"],
        "tone": tone,
        "keywords": extract_keywords(website_content)
    }
```

#### 4. 타입 힌팅

**모든 함수에 타입 힌팅을 사용합니다.**

```python
from typing import List, Dict, Optional, Union
from datetime import datetime

def process_campaign(
    campaign_id: int,
    targets: List[str],
    budget: float,
    start_date: datetime,
    metadata: Optional[Dict[str, str]] = None
) -> Dict[str, Union[str, int, float]]:
    """캠페인 정보를 처리합니다."""
    # 구현
    pass
```

### TypeScript (프론트엔드)

#### 1. 네이밍 컨벤션

```typescript
// 컴포넌트: PascalCase
export const BrandAnalyzer: React.FC = () => {
  // ...
}

// 함수/변수: camelCase
const handleSubmit = () => {
  const userInput = "example";
}

// 상수: UPPER_SNAKE_CASE
const MAX_FILE_SIZE = 10 * 1024 * 1024; // 10MB

// 타입/인터페이스: PascalCase
interface BrandKit {
  colors: string[];
  fonts: string[];
  tone: string;
}
```

#### 2. 주석 작성

```typescript
/**
 * 브랜드 키트를 생성하는 컴포넌트
 *
 * 사용자로부터 로고와 웹사이트 정보를 입력받아
 * AI가 브랜드 컬러, 폰트, 톤앤매너를 자동으로 분석합니다.
 *
 * @param {string} logoUrl - 브랜드 로고 이미지 URL
 * @param {string} websiteUrl - 브랜드 웹사이트 주소
 * @returns {Promise<BrandKit>} 생성된 브랜드 키트
 */
async function createBrandKit(
  logoUrl: string,
  websiteUrl: string
): Promise<BrandKit> {
  // API 호출을 통한 브랜드 분석
  const response = await fetch('/api/brand/analyze', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ logoUrl, websiteUrl })
  });

  // 응답 데이터 파싱
  const brandKit: BrandKit = await response.json();

  return brandKit;
}
```

#### 3. React 컴포넌트 구조

```typescript
import React, { useState, useEffect } from 'react';

// Props 인터페이스 정의
interface BrandAnalyzerProps {
  onComplete: (brandKit: BrandKit) => void;
  initialData?: Partial<BrandKit>;
}

/**
 * 브랜드 분석 컴포넌트
 *
 * 사용자 입력을 받아 브랜드 키트를 생성하고
 * 결과를 부모 컴포넌트로 전달합니다.
 */
export const BrandAnalyzer: React.FC<BrandAnalyzerProps> = ({
  onComplete,
  initialData
}) => {
  // 상태 관리
  const [logoUrl, setLogoUrl] = useState<string>('');
  const [websiteUrl, setWebsiteUrl] = useState<string>('');
  const [isLoading, setIsLoading] = useState<boolean>(false);

  // 제출 핸들러
  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setIsLoading(true);

    try {
      // 브랜드 키트 생성
      const brandKit = await createBrandKit(logoUrl, websiteUrl);
      onComplete(brandKit);
    } catch (error) {
      console.error('브랜드 분석 실패:', error);
    } finally {
      setIsLoading(false);
    }
  };

  return (
    <form onSubmit={handleSubmit}>
      {/* UI 구현 */}
    </form>
  );
};
```

---

## 개발 워크플로우

### 1. 기능 개발 프로세스

```bash
# 1. 최신 main 브랜치 가져오기
git checkout main
git pull origin main

# 2. 새 기능 브랜치 생성
git checkout -b feature/브랜드-분석-개선

# 3. 개발 진행
# 코드 작성 및 테스트

# 4. 커밋
git add .
git commit -m "feat: 브랜드 컬러 추출 알고리즘 개선"

# 5. 푸시
git push origin feature/브랜드-분석-개선

# 6. Pull Request 생성
# GitHub에서 PR 생성 및 리뷰 요청
```

### 2. 커밋 메시지 컨벤션

**Conventional Commits** 형식을 따릅니다:

```
<타입>: <제목>

<본문>

<푸터>
```

**타입:**
- `feat`: 새로운 기능 추가
- `fix`: 버그 수정
- `docs`: 문서 수정
- `style`: 코드 포맷팅 (기능 변경 없음)
- `refactor`: 코드 리팩토링
- `test`: 테스트 추가/수정
- `chore`: 빌드 설정, 패키지 등

**예시:**

```
feat: SNS 마케팅 자동 생성 기능 추가

- 인스타그램, 페이스북, 트위터 연동
- 채널별 최적화된 카피 자동 생성
- 이미지 크기 자동 조정 기능

Closes #123
```

### 3. 코드 리뷰 가이드라인

Pull Request 작성 시:

1. **명확한 제목**: 변경 내용을 한 문장으로 요약
2. **상세한 설명**: 왜 이 변경이 필요한지 설명
3. **테스트 결과**: 테스트 통과 여부 및 스크린샷
4. **체크리스트**:
   - [ ] 테스트 코드 작성 완료
   - [ ] 문서 업데이트 완료
   - [ ] 린팅/포맷팅 통과
   - [ ] 한글 주석 추가 완료

---

## 테스트 작성

### 백엔드 테스트 (pytest)

```python
# tests/unit/test_brand_analyzer.py

import pytest
from backend.services.brand_analyzer import BrandAnalyzer

@pytest.fixture
def brand_analyzer():
    """브랜드 분석기 인스턴스를 생성합니다."""
    return BrandAnalyzer()

def test_컬러_추출_성공(brand_analyzer):
    """
    로고 이미지에서 컬러를 정상적으로 추출하는지 테스트합니다.
    """
    # Given: 테스트용 로고 URL
    logo_url = "https://example.com/logo.png"

    # When: 컬러 추출 실행
    colors = brand_analyzer.extract_colors(logo_url)

    # Then: 결과 검증
    assert len(colors) > 0
    assert all(color.startswith('#') for color in colors)
    assert len(colors[0]) == 7  # #RRGGBB 형식

def test_유효하지_않은_URL_처리(brand_analyzer):
    """
    잘못된 URL이 입력되었을 때 적절히 처리하는지 테스트합니다.
    """
    # Given: 잘못된 URL
    invalid_url = "not-a-valid-url"

    # When & Then: ValueError 예외 발생 확인
    with pytest.raises(ValueError, match="유효하지 않은 URL"):
        brand_analyzer.extract_colors(invalid_url)
```

### 테스트 실행

```bash
# 모든 테스트 실행
pytest

# 커버리지 포함
pytest --cov=backend --cov-report=html

# 특정 파일만 테스트
pytest tests/unit/test_brand_analyzer.py

# 특정 테스트 함수만 실행
pytest tests/unit/test_brand_analyzer.py::test_컬러_추출_성공
```

### 프론트엔드 테스트 (Jest + React Testing Library)

```typescript
// __tests__/components/BrandAnalyzer.test.tsx

import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { BrandAnalyzer } from '@/components/BrandAnalyzer';

describe('BrandAnalyzer 컴포넌트', () => {
  test('입력 필드가 정상적으로 렌더링된다', () => {
    // Given & When: 컴포넌트 렌더링
    render(<BrandAnalyzer onComplete={jest.fn()} />);

    // Then: 입력 필드 확인
    expect(screen.getByLabelText('로고 URL')).toBeInTheDocument();
    expect(screen.getByLabelText('웹사이트 URL')).toBeInTheDocument();
  });

  test('제출 시 onComplete 콜백이 호출된다', async () => {
    // Given: Mock 함수
    const mockOnComplete = jest.fn();
    render(<BrandAnalyzer onComplete={mockOnComplete} />);

    // When: 폼 입력 및 제출
    fireEvent.change(
      screen.getByLabelText('로고 URL'),
      { target: { value: 'https://example.com/logo.png' } }
    );
    fireEvent.change(
      screen.getByLabelText('웹사이트 URL'),
      { target: { value: 'https://example.com' } }
    );
    fireEvent.click(screen.getByText('분석 시작'));

    // Then: 콜백 호출 확인
    await waitFor(() => {
      expect(mockOnComplete).toHaveBeenCalled();
    });
  });
});
```

---

## 배포 프로세스

### 개발 환경 → 스테이징

```bash
# 1. 스테이징 브랜치로 병합
git checkout staging
git merge main

# 2. Docker 이미지 빌드
docker build -t sparklio-backend:staging ./backend
docker build -t sparklio-frontend:staging ./frontend

# 3. 스테이징 서버에 배포
./scripts/deploy/deploy_staging.sh
```

### 스테이징 → 프로덕션

```bash
# 1. 프로덕션 브랜치로 병합
git checkout production
git merge staging

# 2. 버전 태그 생성
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0

# 3. 프로덕션 배포
./scripts/deploy/deploy_production.sh
```

---

## 트러블슈팅

### 자주 발생하는 문제

#### 1. 데이터베이스 연결 오류

```bash
# PostgreSQL 서비스 상태 확인
sudo systemctl status postgresql

# 연결 테스트
psql -h localhost -U sparklio_user -d sparklio_db

# Docker에서 실행 중인 경우
docker-compose logs postgres
```

#### 2. Redis 연결 실패

```bash
# Redis 서비스 확인
redis-cli ping
# 응답: PONG

# Docker에서 Redis 재시작
docker-compose restart redis
```

#### 3. LLM API 할당량 초과

```python
# config/llm_router.py에서 fallback 설정 확인

# 로컬 LLM으로 전환
LLM_ROUTER_CONFIG = {
    "primary": "openai",
    "fallback": ["anthropic", "google", "qwen_local"]
}
```

#### 4. 프론트엔드 빌드 오류

```bash
# node_modules 삭제 및 재설치
rm -rf node_modules package-lock.json
npm install

# 캐시 클리어
npm run clean
npm run dev
```

---

## 추가 리소스

- [프로젝트 구조](../프로젝트_구조.md)
- [API 문서](../api/README.md)
- [아키텍처 설계](../architecture/README.md)
- [기여 가이드](../../CONTRIBUTING.md)

---

**작성일**: 2025-11-13
**작성자**: Sparklio 개발팀
**버전**: 1.0.0
