# Sparklio Editor v2.0 ↔ Multi-Agent Integration

**작성일**: 2025-11-19
**버전**: 1.0
**참조**: [AGENTS_SPEC.md](../../../../docs/PHASE0/AGENTS_SPEC.md), [000_MASTER_PLAN.md](./000_MASTER_PLAN.md)

---

## 📋 목차

1. [개요](#개요)
2. [Agent Families 요약](#agent-families-요약)
3. [메뉴별 에이전트 연계 맵](#메뉴별-에이전트-연계-맵)
4. [Editor 핵심 에이전트 상세](#editor-핵심-에이전트-상세)
5. [Phase별 에이전트 통합 계획](#phase별-에이전트-통합-계획)
6. [API 연동 가이드](#api-연동-가이드)

---

## 개요

### 문서 목적

이 문서는 **Sparklio Editor v2.0 (One-Page Editor)**와 **24개의 Multi-Agent 시스템**이 어떻게 연결되는지 정의합니다.

**핵심 질문에 대한 답:**
- Spark Chat에서 "상품 상세페이지 만들어줘"라고 하면 어떤 Agent들이 호출되나?
- Meeting AI가 회의록을 어떻게 EditorDocument로 변환하나?
- Trend Engine이 자동 생성한 템플릿을 Editor에서 어떻게 편집하나?
- Canvas Studio에서 "AI로 레이아웃 정리"를 누르면 무슨 일이 일어나나?

### 핵심 원칙

1. **Editor는 Headless 패턴**
   - EditorStore (Zustand)가 단일 진실 공급원
   - Agent들은 EditorCommand를 통해 Editor를 제어

2. **Agent는 Backend에서 실행**
   - Frontend는 A2A Gateway를 통해 Agent 호출
   - LLMRouter가 모델 선택 및 비용 최적화

3. **모든 자동 생성은 편집 가능**
   - Agent가 만든 템플릿/문서도 EditorDocument 구조
   - 사용자는 픽셀 단위로 자유롭게 수정 가능

---

## Agent Families 요약

Sparklio의 24개 Agent는 6개 패밀리로 분류됩니다:

### A. Planning / Brief / PM 계열
- **PMAgent**: 전체 워크플로우 조율 및 태스크 분배
- **StrategistAgent**: 캠페인 전략 및 구조 설계
- (추가 계획) **BriefAgent**, **CampaignPlannerAgent**

### B. Copy / Content / Deck 계열
- **CopywriterAgent**: 텍스트/카피/슬라이드 문구 생성
- (추가 계획) **BlogWriterAgent**, **DeckWriterAgent**, **ScriptWriterAgent**

### C. Design / Vision / Video 계열
- **VisionGeneratorAgent**: 이미지 생성 (DALL-E, Stable Diffusion)
- **VisionAnalyzerAgent**: 이미지 품질 평가
- **ScenePlannerAgent**: 광고 영상 씬 구성
- **StoryboardBuilderAgent**: 비디오 스토리보드 생성
- **VideoDirectorAgent**: 영상 제작 및 편집
- **VideoReviewerAgent**: 영상 품질 검사
- (추가 계획) **LayoutDesignerAgent**, **ThumbnailDesignerAgent**

### D. Editor / Meeting / RAG 계열
- **RAGAgent**: 지식 검색 및 컨텍스트 제공
- **ReviewerAgent**: 데이터/콘텐츠 품질 평가
- (신규) **EditorAgent**: 자연어 → EditorCommand 변환
- (신규) **MeetingAIAgent**: 회의록 분석 및 산출물 생성
- (추가 계획) **BrandRAGAgent**, **QAReviewerAgent**

### E. Trend / Template / Analytics 계열
- **TrendCollectorAgent**: 마케팅 데이터 크롤링
- **DataCleanerAgent**: 데이터 정제 및 중복 제거
- **EmbedderAgent**: 임베딩 생성 (OpenAI, BGE)
- **IngestorAgent**: DB 저장 및 캐싱
- **TrendAgent**: 트렌드 분석 및 인사이트
- **TemplateAgent**: 마케팅 템플릿 자동 생성
- **PerformanceAnalyzerAgent**: SNS/광고 성과 분석
- **SelfLearningAgent**: 브랜드 벡터 자동 조정
- **BrandModelUpdaterAgent**: 프롬프트/모델 업데이트
- (추가 계획) **InsightReporterAgent**

### F. System / Router / Cost 계열
- **BudgetAgent**: LLM Token/비용 추적
- **SecurityAgent**: 민감정보 탐지 및 정책 검사
- **ADAgent**: 광고 퍼포먼스 최적화
- (추가 계획) **LLMRouterAgent**, **TaskOrchestratorAgent**, **PublisherAgent**

---

## 메뉴별 에이전트 연계 맵

### Phase 1: Canvas Studio (Editor v2.0 Core)

```
사용자 직접 편집
    ↓
┌─────────────────────────────────────────────┐
│ Canvas Studio (Konva + Zustand)             │
│ - 드래그, 리사이즈, 텍스트 편집              │
│ - 레이어, 정렬, 그룹                         │
│ - 에이전트 개입 없음                         │
└─────────────────────────────────────────────┘

AI 보조 기능 (Phase 1 후반)
    ↓
┌─────────────────────────────────────────────┐
│ "AI로 텍스트 다듬기"                         │
│   → CopywriterAgent (B)                     │
│   → EditorAgent (D): EditCommand 생성       │
│                                              │
│ "레이아웃 자동 정리"                         │
│   → LayoutDesignerAgent (C)                 │
│   → EditorAgent (D): 정렬 명령 생성         │
│                                              │
│ "브랜드 가이드 체크"                         │
│   → BrandRAGAgent (D): 가이드 검색          │
│   → QAReviewerAgent (D): 톤/색상 검증       │
└─────────────────────────────────────────────┘
```

**Phase 1 에이전트 우선순위:**
1. ❌ 에이전트 통합 없음 (순수 Editor 기능만)
2. (Phase 1 후반) CopywriterAgent, EditorAgent 기본 연동

---

### Phase 2: Spark Chat (Brief → Editor)

```
사용자 입력
    ↓
┌─────────────────────────────────────────────┐
│ Step 1: 브리프 정리                          │
│   PMAgent (A)                               │
│   - 입력 정리, 누락 질문 생성                │
│   - 프로젝트 컨텍스트 설정                   │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ Step 2: 전략 수립                            │
│   StrategistAgent (A)                       │
│   - 캠페인 목표/타깃/채널 전략               │
│   - 콘텐츠 구조 제안                         │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ Step 3: 콘텐츠 생성                          │
│   CopywriterAgent (B)                       │
│   - 섹션별 카피 작성                         │
│   - ObjectRole 할당 (headline, body, cta)   │
│                                              │
│   LayoutDesignerAgent (C)                   │
│   - 추천 레이아웃 패턴 선택                  │
│   - TrendPattern 기반 구조                  │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ Step 4: EditorDocument 변환                 │
│   EditorAgent (D)                           │
│   - EditorCommand[] 생성                    │
│   - EditorDocument 조립                     │
│   → Canvas Studio 진입                      │
└─────────────────────────────────────────────┘

* LLMRouterAgent (F): 모든 단계에서 모델 선택/비용 관리
```

**API 호출 예시:**
```typescript
// Frontend → Backend
POST /api/v1/spark-chat/generate
{
  "message": "상품 상세페이지 만들어줘",
  "brandId": "brand-123",
  "context": {
    "productName": "자연주의 스킨케어",
    "targetAudience": "20-30대 여성"
  }
}

// Backend Response
{
  "editorDocument": {
    "id": "doc-456",
    "title": "자연주의 스킨케어 상품 상세",
    "mode": "product-story",
    "pages": [
      {
        "id": "page-1",
        "objects": [
          {
            "id": "obj-1",
            "type": "text",
            "role": "headline",
            "text": "자연에서 온 과학, 피부에 전하는 아름다움",
            "source": {
              "kind": "auto-generated",
              "agentName": "CopywriterAgent"
            }
          }
        ]
      }
    ]
  },
  "metadata": {
    "agents_used": ["PMAgent", "StrategistAgent", "CopywriterAgent", "EditorAgent"],
    "processing_time": 8.5,
    "cost": 0.12
  }
}
```

---

### Phase 3: Meeting AI (Meeting → Editor)

```
회의록 업로드
    ↓
┌─────────────────────────────────────────────┐
│ Step 1: 회의 분석                            │
│   MeetingAIAgent (D)                        │
│   - ASR + 스피커 분리 (음성인 경우)          │
│   - 섹션 파싱 (decisions, actions, insights)│
│   - 콘텐츠 타입 분류 (Product/Pitch/Ad)     │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ Step 2: 전략 매핑                            │
│   StrategistAgent (A)                       │
│   - 회의 결정사항 → 캠페인 전략              │
│   - 채널/형태 적합성 판단                    │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ Step 3: 콘텐츠 초안                          │
│   CopywriterAgent (B)                       │
│   - 회의 내용 → 역할별 텍스트 매핑           │
│                                              │
│   LayoutDesignerAgent (C)                   │
│   - 레이아웃 + 이미지 영역 제안              │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ Step 4: EditorDocument 생성                 │
│   EditorAgent (D)                           │
│   - MeetingToEditorCommand 프로토콜          │
│   - Object Role 자동 할당                    │
│   → Canvas Studio 진입                      │
└─────────────────────────────────────────────┘
```

**MeetingToEditorCommand 프로토콜:**
```typescript
interface MeetingToEditorCommand {
  type: "GENERATE_FROM_MEETING";
  payload: {
    meetingId: string;
    meetingSummary: {
      contentType: "product-detail" | "pitch-deck" | "ad" | "blog";
      sections: {
        role: ObjectRole;          // "headline", "body", "cta-button"
        content: string;            // 실제 텍스트
        priority: number;           // 레이아웃 배치 우선순위
      }[];
    };
    brandContext?: {
      brandId: string;
      designTokens: DesignTokens;
    };
  };
}
```

---

### Phase 4: Asset Library (에셋 관리)

```
┌─────────────────────────────────────────────┐
│ "이 제품으로 썸네일 3개 추천"                 │
│   → VisionDesignerAgent (C)                 │
│   → TemplateAgent (E): 이미지 템플릿 구조   │
│   → PublisherAgent (F): MinIO 업로드        │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ "브랜드 톤에 맞는 아이콘 추천"                │
│   → BrandRAGAgent (D): 브랜드 가이드 검색   │
│   → VisionDesignerAgent (C): 스타일 제안    │
└─────────────────────────────────────────────┘
```

---

### Phase 5: Publish Hub (채널별 퍼블리싱)

```
┌─────────────────────────────────────────────┐
│ "블로그용으로 변환"                           │
│   → BlogWriterAgent (B): HTML 변환          │
│   → EditorAgent (D): EditorDocument → Blog  │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ "SNS 세트 자동 생성"                         │
│   → CopywriterAgent (B): 채널별 카피 변형   │
│   → TemplateAgent (E): 채널별 템플릿 선택   │
│   → EditorAgent (D): 각 SNS용 Document 생성 │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ "광고 플랫폼 발행"                            │
│   → PublisherAgent (F): Meta/Google API 호출│
│   → BudgetAgent (F): 캠페인 비용 체크       │
└─────────────────────────────────────────────┘
```

---

### Phase 6: Admin Console (수동 관리)

```
┌─────────────────────────────────────────────┐
│ 브랜드/프로젝트/템플릿 수동 관리              │
│ - 에이전트 직접 호출 없음                    │
│ - CRUD API만 사용                            │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ 성과 데이터 조회                             │
│   → PerformanceAnalyzerAgent (E)            │
│   → InsightReporterAgent (E)                │
└─────────────────────────────────────────────┘
```

---

### Phase 7: Trend Engine (자동 학습)

```
┌─────────────────────────────────────────────┐
│ Pipeline 1: 데이터 수집                      │
│   TrendCollectorAgent (E)                   │
│   - Instagram, TikTok, Pinterest 크롤링     │
│   - 성공 게시물 수집 (CTR > 5%)             │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ Pipeline 2: 데이터 정제                      │
│   DataCleanerAgent (E)                      │
│   - HTML 제거, 중복 제거, OCR 정제          │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ Pipeline 3: 패턴 분석                        │
│   EmbedderAgent (E)                         │
│   - 텍스트/이미지 임베딩 생성                │
│                                              │
│   TrendAgent (E)                            │
│   - 레이아웃 패턴 추출                       │
│   - TrendPattern 생성                       │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ Pipeline 4: 템플릿 자동 생성                 │
│   TemplateAgent (E)                         │
│   - TrendPattern → TemplateDefinition       │
│   - Object Role 할당                        │
│   - EditorDocument skeleton 생성            │
│                                              │
│   CopywriterAgent (B)                       │
│   - Placeholder 카피 생성                   │
│                                              │
│   VisionDesignerAgent (C)                   │
│   - 이미지 스타일 가이드 생성                │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ Pipeline 5: DB 저장                          │
│   IngestorAgent (E)                         │
│   - TemplateDefinition → Postgres           │
│   - 메타데이터 → Redis 캐싱                  │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ 사용자 서비스에서 "트렌드 템플릿" 사용 가능   │
│ → Canvas Studio에서 전부 편집 가능           │
└─────────────────────────────────────────────┘
```

**자동 학습 계획 예시:**
```json
{
  "learningPlan": {
    "id": "plan-ig-kr-2025-11",
    "name": "Instagram Reels (한국 시장)",
    "schedule": "매일 오전 9시",
    "target": "최근 7일 CTR > 5% 게시물 100개",
    "status": "active",
    "collected": 1234,
    "templates_generated": 45,
    "top_pattern": {
      "layoutPattern": "left-image-right-text",
      "popularityScore": 92,
      "avgCtr": 6.8
    }
  }
}
```

---

### Phase 8: Insight Radar (성과 분석)

```
┌─────────────────────────────────────────────┐
│ 문서/템플릿 사용 통계                         │
│   → InsightReporterAgent (E)                │
│   - 어떤 템플릿이 많이 쓰이는지              │
│   - 어떤 레이아웃이 전환율 높은지            │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ 다음 캠페인 추천                             │
│   → StrategistAgent (A)                     │
│   → TemplateAgent (E)                       │
│   - 성과 데이터 기반 조합 제안               │
└─────────────────────────────────────────────┘
```

---

## Editor 핵심 에이전트 상세

### EditorAgent (신규)

**역할**: 자연어 또는 구조화된 입력을 EditorCommand[]로 변환

**입력 예시:**
```typescript
{
  "task": "convert_meeting_to_document",
  "data": {
    "meetingId": "meeting-123",
    "contentType": "product-detail",
    "sections": [
      {
        "role": "headline",
        "content": "신제품 출시",
        "priority": 1
      }
    ]
  }
}
```

**출력 예시:**
```typescript
{
  "commands": [
    {
      "type": "CREATE_PAGE",
      "payload": {
        "name": "상품 상세 페이지",
        "width": 1920,
        "height": 1080
      }
    },
    {
      "type": "ADD_TEXT",
      "payload": {
        "text": "신제품 출시",
        "x": 100,
        "y": 100,
        "fontSize": 48,
        "role": "headline"
      }
    }
  ]
}
```

**사용 시나리오:**
- Spark Chat → Editor
- Meeting AI → Editor
- 자연어 편집 ("제목을 더 크게" → EditCommand)

---

### MeetingAIAgent (신규)

**역할**: 회의록 분석 및 마케팅 산출물 구조 추출

**입력 예시:**
```typescript
{
  "meetingId": "meeting-123",
  "transcript": "...",
  "outputType": "editor_document"
}
```

**출력 예시:**
```typescript
{
  "meetingSummary": {
    "contentType": "product-detail",
    "sections": [
      {
        "role": "headline",
        "content": "자연에서 온 과학",
        "priority": 1
      },
      {
        "role": "body",
        "content": "피부과학 기반 자연 성분...",
        "priority": 2
      }
    ],
    "decisions": [...],
    "actionItems": [...]
  }
}
```

---

### LayoutDesignerAgent (계획)

**역할**: 레이아웃 패턴 제안 및 자동 정렬

**입력 예시:**
```typescript
{
  "objects": [...],  // 현재 EditorObject[]
  "goal": "auto_align"
}
```

**출력 예시:**
```typescript
{
  "commands": [
    {
      "type": "ALIGN",
      "payload": {
        "objectIds": ["obj-1", "obj-2"],
        "alignment": "left"
      }
    },
    {
      "type": "DISTRIBUTE",
      "payload": {
        "objectIds": ["obj-3", "obj-4", "obj-5"],
        "axis": "horizontal",
        "spacing": 20
      }
    }
  ]
}
```

---

## Phase별 에이전트 통합 계획

| Phase | 메뉴 | 통합 에이전트 | 우선순위 |
|-------|------|--------------|---------|
| **Phase 0-1** | Canvas Studio | ❌ 없음 (순수 Editor) | - |
| **Phase 2** | Spark Chat | PMAgent, StrategistAgent, CopywriterAgent, EditorAgent | **P0** |
| **Phase 3** | Meeting AI | MeetingAIAgent, StrategistAgent, CopywriterAgent, EditorAgent | **P0** |
| **Phase 4** | Asset Library | VisionDesignerAgent, TemplateAgent, PublisherAgent | P1 |
| **Phase 5** | Publish Hub | BlogWriterAgent, PublisherAgent, BudgetAgent | P1 |
| **Phase 6** | Admin Console | PerformanceAnalyzerAgent, InsightReporterAgent | P2 |
| **Phase 7** | Trend Engine | TrendCollectorAgent, DataCleanerAgent, EmbedderAgent, TrendAgent, TemplateAgent, IngestorAgent | **P0** |
| **Phase 8** | Insight Radar | InsightReporterAgent, StrategistAgent | P2 |

**통합 순서:**
1. **Week 4-5 (Phase 2)**: EditorAgent 기본 구현 + Spark Chat 연동
2. **Week 6-7 (Phase 3)**: MeetingAIAgent 구현 + Meeting AI 연동
3. **Week 11-12 (Phase 7)**: Trend Engine 파이프라인 구축 (최우선)

---

## API 연동 가이드

### Frontend → Backend Agent 호출

**공통 엔드포인트:**
```
POST /api/v1/agents/execute
```

**Request Format:**
```typescript
{
  "agent": "CopywriterAgent",
  "task": "generate_headline",
  "payload": {
    "brandId": "brand-123",
    "context": {...}
  },
  "options": {
    "priority": "P0",
    "timeout": 30000,
    "model": "gpt-4"  // optional, LLMRouter가 자동 선택
  }
}
```

**Response Format:**
```typescript
{
  "status": "success",
  "result": {...},
  "metadata": {
    "processing_time": 5.2,
    "tokens_used": 1500,
    "model": "gpt-4",
    "cost": 0.05,
    "agent_version": "2.1"
  },
  "error": null
}
```

### EditorStore에서 Agent 호출 예시

```typescript
// src/modules/editor/store/editorStore.ts

export const useEditorStore = create<EditorState>((set, get) => ({
  // ... existing state

  // AI 보조 기능
  aiRefineText: async (objectId: string) => {
    const obj = get().document?.pages[0].objects.find(o => o.id === objectId);
    if (!obj || obj.type !== 'text') return;

    // Backend Agent 호출
    const response = await fetch('/api/v1/agents/execute', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        agent: 'CopywriterAgent',
        task: 'refine_text',
        payload: {
          text: obj.text,
          tone: 'professional_warm',
          brandId: get().document?.brandId
        }
      })
    });

    const data = await response.json();

    // EditorCommand 실행
    if (data.status === 'success') {
      get().updateObject(objectId, {
        text: data.result.refined_text
      });
    }
  },

  aiAutoLayout: async () => {
    const page = get().document?.pages[get().activePageIndex || 0];
    if (!page) return;

    const response = await fetch('/api/v1/agents/execute', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        agent: 'LayoutDesignerAgent',
        task: 'auto_align',
        payload: {
          objects: page.objects,
          goal: 'balanced_grid'
        }
      })
    });

    const data = await response.json();

    // EditorCommand[] 실행
    if (data.status === 'success') {
      data.result.commands.forEach((cmd: EditorCommand) => {
        get().executeCommand(cmd);
      });
    }
  }
}));
```

---

## 다음 단계

### A팀 (Frontend)
1. [TEAM_A_REQUEST.md](./TEAM_A_REQUEST.md) 업데이트 필요
   - Phase 2-3에 Agent 연동 섹션 추가
   - `/api/v1/agents/execute` API 사용 예시 추가

2. EditorStore에 Agent 호출 함수 추가
   - `aiRefineText(objectId)`
   - `aiAutoLayout()`
   - `aiGenerateFromMeeting(meetingId)`

### B팀 (Backend)
1. [TEAM_B_REQUEST.md](./TEAM_B_REQUEST.md) 업데이트 필요
   - Phase 2-7에 Agent 구현 상세 추가
   - EditorAgent, MeetingAIAgent 구현 가이드

2. Agent Gateway API 구현
   - POST `/api/v1/agents/execute`
   - A2A 프로토콜 연동
   - LLMRouter 통합

3. Trend Engine 파이프라인 구축
   - TrendCollectorAgent → DataCleanerAgent → EmbedderAgent → TrendAgent → TemplateAgent
   - 자동 학습 스케줄러

---

## 참고 문서

- [AGENTS_SPEC.md](../../../../docs/PHASE0/AGENTS_SPEC.md) - 24개 Agent 상세 스펙
- [000_MASTER_PLAN.md](./000_MASTER_PLAN.md) - Editor v2.0 비전 및 시나리오
- [002_DATA_MODEL.md](./002_DATA_MODEL.md) - ObjectRole, TemplateDefinition
- [007_AI_INTEGRATION.md](./007_AI_INTEGRATION.md) - EditorCommand 프로토콜
- [009_TREND_ENGINE.md](./009_TREND_ENGINE.md) - Trend Engine 5단계 파이프라인
- [010_IMPLEMENTATION_ROADMAP.md](./010_IMPLEMENTATION_ROADMAP.md) - Phase별 일정

---

**문서 버전**: 1.0
**마지막 업데이트**: 2025-11-19
